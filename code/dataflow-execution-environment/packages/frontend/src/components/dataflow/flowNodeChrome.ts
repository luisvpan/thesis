/** Contenedor de nodo: no arrastra el lienzo ni captura clics (solo los handles). */
export const FLOW_NODE_SHELL_CLASS = "nodrag nopan pointer-events-none";

/** Handles y otros controles que sí deben recibir toque/clic. */
export const FLOW_NODE_INTERACTIVE_CLASS = "pointer-events-auto";
