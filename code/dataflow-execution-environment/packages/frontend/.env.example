# En dev, Vite hace proxy de `/ws` → http://127.0.0.1:8765 (relay Python / cv-stack).

# WebSocket visión (por defecto mismo host que la página + /ws/vision)
# VITE_VISION_WS_URL=ws://127.0.0.1:8765/ws/vision

# WebSocket toque (por defecto mismo host que la página + /ws/touch)
# VITE_TOUCH_WS_URL=ws://127.0.0.1:8765/ws/touch
