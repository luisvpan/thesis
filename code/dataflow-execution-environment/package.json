{
  "name": "dataflow-execution-environment",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build --filter='*'",
    "dev": "bun run dev --filter='*'",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Lint not configured'"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}